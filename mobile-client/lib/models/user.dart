class User {
  String id, email, username, avatar_url;
  bool isActive;
  String cookie;

  User({
    this.id = '',
    this.email = '',
    this.username = '',
    this.avatar_url = '',
    this.isActive = false,
    this.cookie = '',
  });
}
