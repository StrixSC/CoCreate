export enum ColorType {
    primary,
    secondary,
    background,
}

export const PRIMARY_AND_SECONDARY_SIZE: { width: number, height: number } = { width: 40, height: 40 };

export const PRIMARY_SIZE: { x: number, y: number, width: number, height: number, } = {
    x: 16 ,
    y: 16,
    width: 24,
    height: 24,
};

export const SECONDARY_SIZE: { x: number, y: number, width: number, height: number, } = {
    x: 0,
    y: 0,
    width: 24,
    height: 24,
};

export const BACKGROUND_SIZE: { x: number, y: number, width: number, height: number, } = {
    x: 0,
    y: 0,
    width: 24,
    height: 24,
};
